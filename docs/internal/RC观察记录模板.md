# OpenFish RC 观察记录模板

这个模板用于 `v1.0.0-rc1` 到 `v1.0.0` 之间的短观察窗口。

## 基本信息

- RC 版本：`v1.0.0-rc1`
- 观察开始时间：
- 观察结束时间：
- 记录人：

## 阻塞级问题

如果出现下面任一问题，原则上不应直接升 `v1.0.0`：

- 启动失败
- 重启失败
- Telegram 主链路失效
- 审批流错误处理
- `/ask` 或 `/do` 明显不可用
- `/memory`、`/mcp`、`/model` 出现阻塞级回归

记录：

- 无 / 有
- 详情：

## 非阻塞问题

记录在 RC 期间发现但不阻塞 `v1.0.0` 的问题：

1. 问题：
   - 影响：
   - 是否需要 `v1.0.1` 修复：

2. 问题：
   - 影响：
   - 是否需要 `v1.0.1` 修复：

## 验收回顾

确认以下项目已实际验证：

- `/start`
- 项目切换
- `/status`
- 普通文本按 `/ask`
- `/do`
- 审批流程
- `/memory` 翻页
- `/mcp`
- `/model`
- 启动 / 重启 / 状态

结果：

- 全部通过 / 部分通过
- 补充说明：

## 是否允许转正为 `v1.0.0`

- 结论：允许 / 不允许
- 原因：
- 如果允许，建议发布时间：
- 如果不允许，阻塞项：
